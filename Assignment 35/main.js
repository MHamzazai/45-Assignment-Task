"use strict";
// make a function       
function make_shirt(size, printMessage) {
    console.log(`You Selected "${size}" Size Shirt With "${printMessage}" Print On It !. `);
}
make_shirt("Medium", "Perform Salah");
