//creating a function which return the values 

function city_country(city : string, counry : string) : string{
    return `"${city}, ${counry}"`;
}

// calling the function and giving it values

console.log( city_country("Karachi", "Pakistan"));

// again calling the function with different city country pairs

console.log(city_country("Istanbul", "Turkey"));

// again calling the function with different city country pairs

console.log(city_country("Taif", "SaudiArab"));``