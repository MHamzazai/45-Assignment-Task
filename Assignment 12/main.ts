// storing my favourite transportation mode in a variable

let transport : string[] = ["Bike", "Car", "Motorcycle", "Aeroplane"];

//using for loop

for (let i = 0; i < transport.length; i++) {
    console.log(`I Would Like To Own A, "${transport[i]}"`);
    
}